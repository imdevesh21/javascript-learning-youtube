
// for-in
/*
const games = {
    football: "football",
    cricket: "cricket",
    hockey: "hockey"
}
// for (const [key, value] of Object.entries(games)){
//     console.log(`${key} for ${value}`)
// }

for(const key in games){
    console.log(`${key} games for ${games[key]}`)
}
*/

/*
const programming = ["js", "java", "c++"];

for(const key in programming){
    console.log(`${key} for ${programming[key]}`)
}


const map1 = new Map();

map1.set('IN', "India");
map1.set('US', "United States");
map1.set('AUS', "Australia");

// Convert the Map object to an array of key-value pairs
const mapArray = Array.from(map1);

// Use for...in to iterate over the array
for (const index in mapArray) {
    const [key, value] = mapArray[index];
    console.log(`${key} for ${value}`);
}
*/


/**********Imp array -> for of, object -> for-in********/ 